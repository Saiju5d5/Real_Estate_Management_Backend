spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rems_db
    username: root
    password: saiju5d5
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: update   # Hibernate creates/updates tables
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: never        # 🔥 IMPORTANT FIX

server:
  port: 8080

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
